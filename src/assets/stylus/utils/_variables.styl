// Project variables
//////////////////////////////////////////////////////
//////////////////////////////////////////////////////
//////////////////////////////////////////////////////

// Breakpoints
//////////////////////////////////////////////////////

$breakpoints = {
  min: 320px,
  tablet: 768px,
  desktop: 1024px,

  max: 1280px, // для .wrapper лейаутов как "Наш Север"
}
$breakpoints["mobile--max"] = $breakpoints.tablet - 1
$breakpoints["tablet--max"] = $breakpoints.desktop - 1


// Palette
//////////////////////////////////////////////////////
$colors = {
  nina: #04153E,
}
// Dependencies colors
$colors["text"] = $colors.nina


// Effects
//////////////////////////////////////////////////////
$effects = {
  duration: 0.1s,
}
